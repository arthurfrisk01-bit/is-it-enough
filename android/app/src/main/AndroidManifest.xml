<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      UsageStatsManager：需要用户前往系统“使用情况访问”手动授权，
      PACKAGE_USAGE_STATS 是 signature|privileged 权限，不能仅靠安装时授予。
      tools:ignore 用于压制 IDE 对受保护权限的误报。
    -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- 全局悬浮窗：flutter_overlay_window / TYPE_APPLICATION_OVERLAY 需要 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Android 8+ 后台前台服务，用于保持轮询监听 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Android 13+ 通知权限，辅助提醒/后台保活通知 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 强提醒全屏通知（Overlay 不可用时的系统级全屏打断兜底） -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 开机自启（可选，用于重启后恢复监听） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name="${applicationName}"
        android:label="够了吗"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/LaunchTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter 引擎相关 meta-data -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
          flutter_overlay_window 全局悬浮窗服务。
          类名以当前 pub.dev 上 flutter_overlay_window 0.4.5 插件源码为准。
        -->
        <service
            android:name="flutter.overlay.window.flutter_overlay_window.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="show in-app reminder overlay for digital wellbeing" />
        </service>
    </application>

    <queries>
        <!-- 用于 android_intent_plus 跳转系统设置/厂商页面 -->
        <intent>
            <action android:name="android.settings.USAGE_ACCESS_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.action.MANAGE_OVERLAY_PERMISSION" />
        </intent>
    </queries>
</manifest>
